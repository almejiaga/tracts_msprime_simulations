# tracts_msprime_simulations
This si the repository containing msprime simulations for admixture under different scenarios and their corresponding msprime backward simulation model.

Each Jupyter notebook contains the code necessary to replicate the simulations performed on msprime, and the corresponding parameters to infer the tract length ditribution using tracts.
